# 🗺️ AddPlace Component

A React client component for adding new places with location, images, and category options.

## ✨ Features
- Fetches city list from API (`/city/all_cities`)
- Interactive **Leaflet map** to pick latitude and longitude
- Form inputs for name, description, and city
- Category selection and “featured” checkbox
- Uploads main and secondary featured images
- Sends form data to `/place/add_place` endpoint with token authorization

## 🧱 Built With
- **React (useState, useEffect)**
- **Leaflet + React-Leaflet**
- **Tailwind CSS** for styling
